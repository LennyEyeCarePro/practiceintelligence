{
  "name": "practiceintelligence-local",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Practice Intelligence — Local server (runs on localhost)",
  "scripts": {
    "start": "node server.js",
    "setup": "npm install && node setup-public.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
